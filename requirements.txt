# =====================================================
# TDTC AI 파이프라인 Python 의존성
# =====================================================

# --- Web / API 서버 ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # FastAPI 파일 업로드(Form, UploadFile)
pydantic==2.7.4

# --- HTTP 클라이언트 ---
requests==2.32.3

# --- 환경변수 ---
python-dotenv==1.0.1

# --- 데이터베이스 (Supabase PostgreSQL 직접 접속) ---
psycopg2-binary==2.9.9

# --- AI / 딥러닝 (CSRNet) ---
torch==2.3.1
torchvision==0.18.1

# --- 컴퓨터 비전 ---
opencv-python==4.10.0.84

# --- 수치 연산 / 데이터 처리 ---
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# --- 공간 분석 ---
shapely==2.0.5

# --- 진행률 표시 ---
tqdm==4.66.5

# --- 비동기 WebSocket (ai_server.py 통합 WebSocket 스트리밍) ---
websockets==12.0
